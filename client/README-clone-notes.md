Clone notes for omnatigray.org

This scaffold duplicates the high-level structure of the example site:
- Hero with intro + CTA
- Selected work / portfolio grid
- About section
- Footer with links

Content and images are intentionally placeholder text. Replace with your own copy and assets.

To run locally:

```bash
cd client
npm install
npm run dev
```

To serve on the backend port (4000):

1. Build client: `npm run build`
2. Update `server/index.js` to serve `client/dist` (I can patch that for you)
3. Start server: `cd server && npm run start`

If you want me to fetch the live site's content and attempt a closer copy, tell me which exact pages or assets you want copied. I will not copy large copyrighted text without your confirmation.
